pipeline {
    agent any

    stages{
        
        stage('preapare Artifact') {
            steps {
                sh '''
                 zip ../frontend.zip *
                 '''
            }
        }
    }
}